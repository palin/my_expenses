%table.table.table-bordered.table-hover.row
  %thead
    %th #
    %th Payed at
    %th Price
    %th Name
    %th Description
    %th Category
    %th Added at

  - @expenses.each_with_index do |ex, i|
    %tr
      %td= i + 1
      %td= ex.payed_at.to_date
      %td= ex.price
      %td= ex.name
      %td= ex.description
      %td= ex.category.name if ex.category
      %td= ex.created_at.strftime("%Y-%m-%d %H:%M")
      %td.edit
        = link_to(edit_dashboard_expense_path(ex), class: 'btn btn-info') do
          Edit
          %span.glyphicon.glyphicon-pencil{'aria-hidden' => true}
      %td.delete
        = link_to(dashboard_expense_path(ex), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger') do
          Delete
          %span.glyphicon.glyphicon-remove{'aria-hidden' => true}
